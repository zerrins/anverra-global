# Visual Baselines

This directory is reserved for future Playwright visual snapshot baselines (`*-expected.png`).
Currently, no baselines exist because the UI is undergoing a redesign.

Once the redesign is stable, baseline screenshots will be committed here.
